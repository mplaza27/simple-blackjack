{
  "name": "simple-blackjack",
  "version": "1.0.0",
  "description": "Browser-based blackjack trainer for learning optimal play and card counting",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npx playwright test",
    "serve": "python3 -m http.server 8080 --directory static"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "@playwright/test": "^1.50.0"
  }
}
